/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['img.shields.io', '*.vercel.app', 'anujkarn.dev'],
  },
};

module.exports = nextConfig;
